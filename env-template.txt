# Environment Variables Template
# Copy this file to .env and fill in your actual values

# Veeva Vault Configuration
VAULT_DOMAIN=y-prime-quality.veevavault.com
VAULT_API_VERSION=v25.2
VAULT_USERNAME=<your_vault_username>
VAULT_PASSWORD=<your_vault_password>

# Supabase Configuration (AccelerQA Reg Intel database)
# Get these from your Supabase project → Settings → API
SUPABASE_URL=https://<your-project-ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<your-service-role-key>
# Get DATABASE_URL from Supabase → Settings → Database → Connection string (Transaction pooler recommended)
DATABASE_URL=postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres

# OpenAI Configuration (for document summarization)
OPENAI_API_KEY=<your_openai_api_key>

# OpenAI Fallback Control (set to 'true' to disable gpt-4o-mini fallback)
DISABLE_OPENAI_FALLBACK=false

# GovInfo CFR API
GPO_API_KEY=<your_govinfo_api_key>

# Auth0 Configuration
VITE_AUTH0_DOMAIN=<your_auth0_domain>
VITE_AUTH0_CLIENT_ID=<your_auth0_client_id>

# Auth0 Management API Configuration (for profile updates)
AUTH0_MGMT_DOMAIN=<your_auth0_domain>
AUTH0_MGMT_CLIENT_ID=<your_management_api_client_id>
AUTH0_MGMT_CLIENT_SECRET=<your_management_api_client_secret>

# Groq Configuration (for chat)
GROQ_API_KEY=<your_groq_api_key>

# Security Configuration
# Rate Limiting (optional - defaults: 100 requests per 60 seconds)
CHAT_RATE_LIMIT_MAX_REQUESTS=100
CHAT_RATE_LIMIT_WINDOW_MS=60000

# CORS Configuration (optional - defaults to * if not set)
# Comma-separated list of allowed origins
ALLOWED_CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com